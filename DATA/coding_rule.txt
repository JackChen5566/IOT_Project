命名規範：

使用有意義且描述性的變數和函數名。
遵循一致的命名約定，如駝峰命名法（camelCase）或底線命名法（snake_case）。
APP頁面名稱為XXX_PAGE(LOGIN_PAGE,MAIN_PAGE)


縮排和格式化：

使用一致的縮排風格，4個空格或一個Tab。
每排長度不超過81個字元

註釋：

編寫清晰的註釋，解釋程式碼的關鍵部分和複雜的邏輯。
避免過多或無效的註釋，註釋應該為程式碼提供額外的信息，而不是重複程式碼本身。


函數與方法：

函數應該只做一件事，並保持簡短，通常不超過一個螢幕。
避免在函數中使用全域變量，盡量傳遞參數。